{
  "name": "rainboot.css",
  "version": "1.0.0",
  "license": "Apache-2.0",
  "scripts": {
    "start": "nodemon --watch src --ext scss build.mjs",
    "build": "node build.mjs",
    "clean": "rimraf build"
  },
  "devDependencies": {
    "@cloudscape-design/design-tokens": "^3.0.31",
    "bootstrap": "^5.3.2",
    "esbuild": "^0.19.11",
    "esbuild-sass-plugin": "^2.16.1",
    "nodemon": "^3.0.2",
    "rimraf": "^5.0.5"
  }
}
