{
  "name": "rainboot.css",
  "version": "1.0.0",
  "license": "Apache-2.0",
  "scripts": {
    "start": "nodemon --watch src --ext scss esbuild.mjs",
    "build": "node esbuild.mjs",
    "clean": "rimraf build/rainboot.css build/rainboot.min.css"
  },
  "devDependencies": {
    "@cloudscape-design/design-tokens": "^3.0.31",
    "bootstrap": "^5.3.2",
    "esbuild": "^0.19.11",
    "esbuild-sass-plugin": "^2.16.1",
    "nodemon": "^3.0.2",
    "rimraf": "^5.0.5"
  }
}
