{
  "name": "rainboot.css-demo",
  "version": "1.0.0",
  "license": "Apache-2.0",
  "scripts": {
    "start": "nodemon --watch src --ext * build.mjs",
    "build": "node build.mjs"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
